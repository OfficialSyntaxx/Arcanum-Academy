{
  "name": "@alderfell/admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && node ../../tools/scripts/clean-build-output.mjs admin && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  },
  "dependencies": {
    "@netlify/identity": "^2.0.0"
  }
}
